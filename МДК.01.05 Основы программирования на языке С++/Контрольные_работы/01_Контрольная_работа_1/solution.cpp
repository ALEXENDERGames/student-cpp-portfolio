Контрольная №1 
Часть А
Массив в плюсах это когда куча элементов одного типа подряд в памяти лежат
статический это типа int a[20]; размер сразу известен
а динамический через new int* p = new int[n]; уже в рантайме размер можно задать
вектор он сам растет когда места мало push_back кидаешь и все
а array размер всегда фиксированный на этапе компиляции известен
array быстрее и памяти в куче не берет
беру вектор когда не знаю сколько надо элементов а array когда например матрица 4 на 4 точно
стринг это класс для строк нормальных
внутри по идее указатель на массив чаров размер и capacity как у вектора наверное
char* data, size_t len, size_t cap типа того
двумерный массив можно так
int mas[10][5]; самый простой
vector<vector<int>> v(10, vector<int>(5));
или int** m = new int*[r]; потом цикл и new int[c] для каждой строки
методы у string которые часто юзаю
size() или length() — сколько символов
+= "привет" или append
push_back('!')
pop_back() убирает последний
find("кот") возвращает позицию или -1 типа
substr(3,4) кусок строки берет
clear() все стирает
front() первый символ back() последний

Часть Б

добавить 42 в конец тремя способами
v.push_back(42);
v.emplace_back(42);
v.insert(v.end(), 42);
[] если индекс за границей то просто мусор или краш неопределенное поведение
.at() проверяет и если что exception кидает out_of_range
поэтому at лучше когда не уверен в индексе или в контесте где краш недопустим
как удалять из вектора
pop_back() последний убирает
erase(v.begin()+3) удаляет по позиции и все сдвигает
clear() все чистит размер ноль
resize(5) если был 10 то последние 5 удалит
std::find со строкой
честно говоря std::find ищет один символ а не подстроку
поэтому для подстроки лучше s.find("hello")
а если прям алгоритм то std::search(s.begin(), s.end(), "test", "test"+4);
код опасный потому что после push_back вектор может перевыделить память
и тогда ptr будет показывать в никуда уже
поэтому *ptr может быть 20 а может мусор а может и программа упадет

Часть В

двумерка 5 на 8

C++int arr[5][8];

for(int i=0; i<5; i++){
    for(int j=0; j<8; j++){
        arr[i][j] = i*10 + j;
    }
}

for(int i=0; i<5; i++){
    for(int j=0; j<8; j++){
        cout << arr[i][j] << "   ";
    }
    cout << endl;
}

заменить beautiful на amazing

C++text.replace(text.find("beautiful"), 9, "amazing");
вроде все Owo
